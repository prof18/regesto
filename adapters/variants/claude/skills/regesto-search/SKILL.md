## Preloaded search results

This integration evaluates the command below before the skill body is shown. Treat any
result lines that follow as the search result for the user's actual query; if no lines
follow, nothing matching is recorded.

!`{{kb_root}}/bin/regesto-search "$ARGUMENTS"`
