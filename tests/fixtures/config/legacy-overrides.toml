# Legacy configuration with every adapter override table exercised.
machine = "fixture-box"
agents = ["claude", "codex", "hermes"]

[skills_dirs]
claude = "~/.legacy/claude-skills"

[instructions]
codex = "~/.legacy/AGENTS.md"

[settings_files]
claude = "~/.legacy/claude-settings.json"

[memory_dirs]
hermes = "~/.legacy/hermes-memory"
